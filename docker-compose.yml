services:
  app:
    image: philippspinnler/av-plan:latest
    build: .
    container_name: av-plan
    ports:
      - '3000:3000'
    volumes:
      - ./data:/data
    environment:
      - ORIGIN=${ORIGIN:-http://localhost:3000}
      - DATABASE_PATH=/data/app.db
      - BODY_SIZE_LIMIT=1048576
      - TZ=Europe/Zurich
    restart: unless-stopped
