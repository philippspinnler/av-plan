services:
  app:
    build: .
    container_name: abendmahlsversammlung
    ports:
      - '3000:3000'
    volumes:
      - ./data:/data
    environment:
      - ORIGIN=${ORIGIN:-http://localhost:3000}
      - DATABASE_PATH=/data/app.db
      - BODY_SIZE_LIMIT=1048576
    restart: unless-stopped
